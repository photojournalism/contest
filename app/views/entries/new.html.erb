<% title 'Contest Entry' %>

<div class="row">
  <div class="col-md-8 col-md-offset-2">
    <h1 class="heading"><%= yield(:title) %></h1>
    <%= render :partial => 'shared/messages' %>

    <% if @contest.is_open? %>
      <p><em>The following form is used to create a new entry for the <strong><%= @contest %></strong>. You may use this form as many times as you'd like to enter multiple categories.</em></p><br>

      <h4>Order Number</h4>
      <p>Use the box below to enter your order number for your payment on <a href="http://photojournalism.eventbrite.com" target="_blank">EventBrite</a>. If someone paid on your behalf, please enter the name of the person or company that paid on your behalf.</p>

      <div class="form-group">
        <input type="text" class="form-control" placeholder="Order Number, e.g., 123456789 or Associated Press" id="entry-order-number"><br>
      </div>

      <h4>Category</h4>

      <p>Use the dropdown list below to select the category you would like to enter. A description of the category is shown upon selection.</p>

      <select class="form-control" id="category-select">
      <% @categories.each do |category| %>
        <option value="<%= category.id %>"
          data-category-description="<%= category.description %>"
          data-category-type="<%= category.category_type.name %>"
          data-category-type-description="<%= category.category_type.description %>"
          data-minimum-files="<%= category.category_type.minimum_files %>"
          data-maximum-files="<%= category.category_type.maximum_files %>"
          data-has-url="<%= category.category_type.has_url %>"
          data-file-types="<% category.category_type.file_types.each_with_index do |t, i| %><%= "#{t.extension}," %><% end %>"><%= category.name %></option>
      <% end %>
      </select>
      <p id="category-description">
        <strong><span id="category-description-title"><%= @categories.first.name %></span></strong><br>
        <span id="category-description-description"><%= @categories.first.description %></span><br><br>
        <em><span id="category-type-description"><%= @categories.first.category_type.description %></span></em>
      </p>
      <br>
      <button type="button" class="btn btn-primary btn-lg pull-right" id="entry-continue-button">Continue&nbsp;&nbsp;<i class="glyphicon glyphicon-arrow-right"></i></button>
      <div class="clearfix"></div><br>
    <% end %>
  </div>
</div>