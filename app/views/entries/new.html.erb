<div class="row">
  <div class="col-md-8 col-md-offset-2">
    <h1 class="heading">New Entry</h1>

    <h4>Category</h4>

    <p>Use the dropdown list below to select the category you would like to enter. A description of the category is shown upon selection.</p>

    <select class="form-control" id="category-select">
    <% @categories.each do |category| %>
      <option value="<%= category.id %>" data-category-description="<%= category.description %>"><%= category.name %></option>
    <% end %>
    </select>
    <p id="category-description">
      <strong><span id="category-description-title"><%= @categories.first.name %></span></strong><br>
      <em><span id="category-description-description"><%= @categories.first.description %></span></em>
    </p>
  </div>
</div>